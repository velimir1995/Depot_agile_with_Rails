<section class="depot_form">
  <h1>Editing Order</h1>
  <%= render 'form', order: @order %>
</section>

<%= link_to 'Show', @order %> 
<%= link_to 'Back', orders_path %>
<%= javascript_pack_tag("pay_type") %>